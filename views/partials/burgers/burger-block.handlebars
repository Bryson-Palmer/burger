<li>
    {{burger_names}}

    <button class="devoure-it" data-id="{{id}}" data-devoured="{{devouring}}">
        {{#if devouring}}Devoured!
        {{else}}Devour it!{{/if}}
    </button>

    <button class="burger-obliviate" data-id="{{this.id}}">Burger Obliviate!</button>
</li>